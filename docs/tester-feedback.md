# Interlude 0.5.0 feedback

Build: 4da4458323308d8f073ffaa96d4bb49e1fde5f9e
Date:
OS and version:
Chip / architecture (Windows x64 / Apple Silicon / Intel Mac):
Browser and version:
Assistant and version, if visible:
Surface (Codex desktop / Claude Code local / Claude chat web / Claude Code web / Codex web / Claude desktop Chat / Claude desktop Cowork):
Media service and page type (regular video / Shorts / feed / music):
One or two monitors? Number of assistant windows?
Website or desktop monitoring? Fun or Locked-in mode?

Record PASS / FAIL / NOT TESTED — do not leave an untested case as PASS.

- Install / extension load:
- Setup and working detection:
- Completion pauses picture AND sound:
- Correct AI tab/window returns:
- Second prompt resumes only Interlude-owned media:
- A manually paused video stays paused:
- Real permission/question pauses and resolves:
- Locked-in lesson and answer:
- Turn off / Release playback / manual Play:
- Reconnect / navigation / cancellation:
- Quit/reopen starts desktop disarmed (desktop only):
- Multiple tasks / two displays (optional):
- Uninstall and unrelated hook preservation (desktop only):

Problem title:
Steps to reproduce:
Expected result:
Actual result (did playback really stop? which window came forward?):
How often (once / sometimes / every time):
Exact visible status/error (remove private information):
Recovery used and whether it worked:
Desktop debug summary, if available:
Optional redacted screenshot:

Stop testing automatic handoffs for unexpected playback, the wrong window or a hold that cannot be released. Return this file to the person who sent the package. Do not include prompts, tokens, account credentials, private file paths or connection.json.
